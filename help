# Generate gRPC files for .proto file
protoc --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative ad/ad.proto

# Check dependencies
go mod tidy

# build
go build

# run
./tycoon_services